# Todo App Made with Nextjs 13

## Notes

- To initialize app with prisma

```bash
npx prisma init --datasource-provider sqlite
```

- To run migration on prisma

```bash
npx prisma migrate dev --name init
```
